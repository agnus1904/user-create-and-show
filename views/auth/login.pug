extends ../layouts/common

block content
    h1 Login

    if errors
        each error in errors
            .alert.alert-danger= error

    form(aciont="", method="POST")
        .form-group
            label(for="inputmail") Mail
            input#inputmail.form-control.col-6(type="email", name="email",size="20", value=(values) ? values.email : '' )

        .form-group
            label(for="inputpassword") Password 
            input#inputpassword.form-control.col-6(type="password", name="password",size="20" value="")


            button.btn.btn-danger.mt-2 Login